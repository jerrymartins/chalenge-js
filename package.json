{
  "scripts": {
    "test:all": "jest",
    "test:chalenge:sum": "jest -t 'sum'",
    "test:challenge:capture-pokemon": "jest -t 'capture the pokemon'",
    "test:challenge:send-pokemon": "jest -t 'send the pokemon'"
  },
  "devDependencies": {
    "jest": "26.6.3"
  }
}
